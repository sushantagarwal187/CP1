#include <stdio.h>

int main()
{
    float height, length, area;

    printf("Enter height: ");
    scanf("%f", &height);

    printf("Enter base: ");
    scanf("%f", &length);

    area = (height * length) / 2;

    printf("Area of Triangle = %.2f", area);

    return 0;
}
