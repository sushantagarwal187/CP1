#include <stdio.h>  // HEADER FILE

void main()
{
  /* Program to add 2 numbers. */
  int a,b,c;
  printf("Enter 2 values:");
  scanf("%d %d",&a,&b);
  c = a + b;
  printf("Answer = %d\n",c);

}