#include <stdio.h>

int main()
{
    float gross_sales, discount, net_sales;

    printf("Enter gross sales: ");
    scanf("%f", &gross_sales);

    discount = 10.0 / 100 * gross_sales;
    net_sales = gross_sales - discount;

    printf("Discount = %.2f\n", discount);
    printf("Net Sales = %.2f", net_sales);

    return 0;
}
